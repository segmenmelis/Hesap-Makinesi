 aslıgul uçar tarafından mod alma islemi yapldı.
 
 
 void mod(int sayi,int mod, int sonuc)
{
	int sayi,mod;
		printf("modu alınacak sayiyi giriniz:",sayi);
		scanf("%d",&sayi);
		printf("sayinin modunu girin:",mod);
		scanf("%d",&mod);
		sonuc=sayi%mod;
    	printf("sonucunuz: %d(%d)=%d",sayi,mod,sonuc);
    }
